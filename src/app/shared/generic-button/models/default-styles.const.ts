import { ButtonConfig } from "./button.config";

export const DEFAULT_STYLES: ButtonConfig = {
    textColor: '#000000',
    backgroundColor: '#FFFFFF',
    fontSize: 16,
    borderWidth: 0,
    borderStyle: 'solid',
    borderColor: '#CCCCCC'
}
